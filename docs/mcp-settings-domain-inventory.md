# Spec 0231 settings and diagnostic-action implementation inventory

Current implementation inventory for `/projects/nickel`, updated 2026-09-11 after host, cross-target, Proton, and owned native-fixture validation. This remains an implementation checklist, not a completion claim. Spec 0231 explicitly requires typed read/change of nonprotected settings and safe production-owned diagnostic actions. UI interaction is not a substitute for a typed transaction contract, particularly where the Settings application contains protected controls.

## Existing MCP scope

- `shell_behavior_transaction`: bar display scope, bar window scope, configured desktop count. `diagnostic_snapshot.shell_behavior` reads the domain. Production paths: `state.rs::commit_shell_behavior_transaction` / `remote_diagnostics.rs::remote_shell_behavior_transaction`; `remote_settings.rs::PreparedShellBehavior` stages `ShellSettings`. The appearance patch adds a final cancellation/emergency/deadline recheck to the existing rename boundary.
- Integrated appearance transactions: theme, accent hue/intensity, reduced transparency preference, animation preference; typed `read_appearance`/`appearance_transaction`; same shell-settings authority. Wallpaper and file icons use separate typed production transactions. Appearance does not include OS-wide color-scheme publication or all remaining settings.
- `diagnostic_action` has `Repaint`, `RefreshScene`, `StartFrameTrace`, `StopFrameTrace`, exact-output `IdentifyOutput`, and typed `RefreshPlatformStatus` domains (`nickel-remote-control/src/diagnostics.rs`). `diagnostic_snapshot` creates a bounded response, not an arbitrary file export.

## Settings domains still requiring explicit coverage

| Domain and local values | Authoritative existing read/write/apply path | Remaining typed contract and exclusions | Evidence needed |
|---|---|---|---|
| Display layout: enabled outputs, primary, position, output scale | `read_display_layout` and `display_layout_transaction` use the production Linux output owner with exact output IDs/generations, complete bounded membership, topology/prior CAS, and Apply/Keep/Revert. The owner retains the prior layout for the same lease and automatically reverts after 15 seconds. Local Settings application supersedes remote recovery. Windows supplies owner-backed reads and explicitly rejects mutation because it has no production display-configuration owner. | Outcomes distinguish requested, confirmed, and revert-failed owner state; they do not claim pixel presentation. Test-output creation remains excluded from production hardware configuration. | Live DRM mixed-DPI move/scale/primary, unplug and native-failure recovery, expiry/emergency during a pending apply, direct owner-timer acceptance, and presented pixels. Windows mutation remains unavailable until a production owner exists. |
| Application scaling / toolkit compatibility | `ApplicationScaleSettings` in `nickel-core/src/dpi.rs`: FollowNickel, Unchanged, Custom(Scale120), owned previous/applied GTK/Qt records. Linux uses the full typed GTK/Qt transaction owner. Windows now supports bounded policy read/change through the production journal, reports both external adapters unavailable, and preserves every existing ownership/pending record. Both owners use generation/prior and underlying revision checks; Windows additionally rechecks protected focus, physical/shared input, lease/emergency epoch and one request deadline at the write-through replacement boundary. | Windows has no production native GTK/Qt global setting, so it must continue to report those adapters unavailable rather than exposing environment variables, registry paths or arbitrary config files. | Native Windows policy transaction and physical-input/revocation acceptance; Linux GTK and Qt read-back, launch environment, running-app restart distinction, external writer between prior/apply, restoration only of still-owned values, deadline/revoke while backend delayed. |
| Wallpaper image and position | `read_wallpaper` and `wallpaper_transaction` expose positions plus an approved image catalog using observation-local opaque IDs. Discovery is confined to bounded canonical roots; selection rejects paths and content fields and enforces file, dimension, pixel and decoder-allocation budgets before a generation/file-revision CAS commit. Linux and Windows owners continuously check the permit and deadline around discovery and decode; failed decode retains the last successfully decoded wallpaper. | Arbitrary paths, image bytes, configured paths and unrestricted scanning remain excluded. The result distinguishes decoded selection and requested cache reload from pixel presentation. | Native output aspect ratios/span, inaccessible-file and external-replacement races, cache/theme invalidation, and presented pixels. Portable tests cover malformed and oversized images, catalog confinement, stale selection and retained prior wallpaper; Windows native persistence/cache/presentation remains open. |
| File icon provider and theme | Implemented as `read_file_icons` / `file_icons_transaction` with a bounded platform-owned installed-theme catalog, Nickel/System enum, stable ShellSettings lock and revision CAS. Unavailable configured IDs remain observable; malformed/path-like legacy values remain preserved internally but are not disclosed. Linux uses its production settings notification; Windows directly advances the launcher icon generation and requests a live inventory/cache refresh after commit. | Native presented-icon confirmation remains open. The result reports refresh requested rather than pixel confirmation. | Switch provider/theme against actual desktop/file icons; removed/reinstalled theme, image-cache generation changes, stale CAS and revocation. |
| Terminal preferences | `read_terminal_presentation` and `terminal_presentation_transaction` expose the visual/behavior subset through locked staged revision CAS. `read_terminal_launch_policy` and `terminal_launch_policy_transaction` separately expose a closed installed-shell catalog and owner-contained directory choices through opaque identities. Both Linux and Windows owners retain catalog/file revisions, exact prior state, generation CAS, continuous permit/deadline checks and report that changes apply only to newly created terminals. | Executable strings, commands and arbitrary paths remain excluded. Only platform-discovered installed shells and canonical directories inside the owner's home may be selected; unavailable legacy values remain hidden rather than becoming write authority. | Native new-terminal launch/rendering, unchanged existing terminals, removed shell/directory identities, external replacement, physical-input arbitration and revocation. Portable schema, CAS, hidden-value and cancellation coverage passes; native Windows execution remains open. |
| On-screen-keyboard preference | `OptionalFeatureSettings.on_screen_keyboard` + monotonic generation, `KeyboardPreference` Automatic/Enabled/Disabled; local preference update and reload; runtime query `OnScreenKeyboardSnapshot`; `ConfigureOnScreenKeyboard` is the existing controller-facing session command. `read_keyboard_preference` and `keyboard_preference_transaction` use the shared bounded loader and a keyboard-only staged revision CAS that preserves Codex fields. The response distinguishes committed configuration from runtime generation acknowledgement. On Windows the production `LiveShell` owner resolves the process override and native digitizer presence, owns the runtime generation and updates panel/keyboard visibility after commit. | Controller-supplied `environment_override`, visibility, docking, height, recipients, epochs and input are absent from and rejected by the schema. Pending configure, recipient/input teardown and enablement policy remain owner-controlled. The environment override is observable only as a boolean and may override the configured preference. | Native automatic touchscreen detection, env-forced policy, closing existing OSK without stuck input, local input arbitration and trusted assistive-tech accessibility. Schema exclusion, hidden-field preservation, cancellation, local replacement and pending acknowledgement have portable tests. Windows native interaction remains open. |
| Codex optional-feature enablement and source | `read_codex_preference` and `codex_preference_transaction` expose policy-aware enablement through the shared bounded `OptionalFeatureSettings` writer. The compositor directly creates or tears down its existing internal Codex host and hidden project menu, reports runtime generation/pending state, and rejects disablement while chat windows are active. The schema reports only whether a custom executable is configured; it neither returns nor accepts its path, label, credentials, account, project, thread, or backend payload. | **Source selection remains excluded.** Custom executable paths/source selection cannot grant arbitrary program execution, alter update trust, or expose account credentials. The remote transaction preserves the selected source and every OSK field. ForceEnabled/ForceDisabled policy remains owner-controlled. | Portable schema, hidden-field preservation, cancellation, file-ABA and real in-process compositor create/hidden-menu/complete-teardown checks pass. Native presented-pixel and active-chat rejection acceptance remain open; supported-source probing and failure recovery are separate from enablement. |
| Default applications and shell launch preferences | `read_default_association` and `default_association_transaction` expose bounded MIME/extension/scheme targets and verified handler catalog IDs. The service performs exact generation/prior CAS, refreshes the native target and catalog before mutation, filters protected Nickel handlers, and re-queries the effective handler after Linux writes. Windows returns `native_consent_required` and opens the native consent path without writing protected `UserChoice`. | Executable commands, source paths, icon paths, broad registry/config writes, and authentication/permission handlers remain excluded. Shell terminal/file-manager launch preferences are separate typed domains. | Live Linux mutation was not run because it would alter the host user's association. Windows registry/catalog discovery and Default Apps consent need native Windows acceptance; handler-removal and cancellation races have portable coverage. |
| Wi-Fi and Bluetooth ordinary controls | `read_device_settings` and `control_device_settings` expose secret-free power/discovery observations through the production NetworkManager/BlueZ owner. Full-debug/full-session authority, exact native observation generations and prior values, local-input arbitration, and a final permit/deadline recheck guard each write. Discovery is standing ownership retired by Stop, last-watch loss, or authority loss. | Passwords, pairing PINs, stored network secrets, device/network selection, permission granting, and authentication bypass are absent. Native setters cannot provide atomic CAS against unrelated external clients; Nickel revalidates immediately before writing and reports requested/uncertain if readback disagrees. | Private NetworkManager/BlueZ native fixtures pass power, discovery lifetime, stale-state, delayed cancellation, and last-watch teardown. Real hardware, connection loss caused by radio power, busy/unavailable adapters, and Windows remain open. |
| Audio ordinary controls | The same typed device tools expose bounded output volume and mute state through the production PipeWire owner. Exact native observation generations/prior values, local-input arbitration, permit/deadline checks, and readback apply; the trusted Remote AI Control audible indicator is a separate protected preference. | Device names/IDs and audio payloads are excluded. Test playback is not performed. Native PipeWire cannot guarantee atomic CAS against unrelated clients after the final revalidation, so readback disagreement is reported truthfully. | Owned dummy-sink native tests pass volume, mute/unmute, external stale-generation rejection, and delayed local-input cancellation. Real sinks, backend failure, and Windows remain open. |
| Printers/removable devices | `read_peripherals` exposes bounded, scrubbed printer, job and removable-volume status through observation-local opaque IDs. `control_peripheral` allowlists only changing the default to an existing printer and cancelling an existing print job, using exact prior state, a one-use generation, shared-input ownership and continuous cancellation/deadline checks on Linux. | Printer addresses and labels, paths, credentials, add/remove/test-page operations, mounts, ejects and cleanup locations are excluded. Windows reports mutation unavailable because its current spooler boundary cannot provide safe cancellable ownership. | Native Linux MCP lease fixture, external-state races and provider loss; native Windows implementation and acceptance after a cancellable production owner exists. Portable platform and owner tests cover bounds, scrubbing, replay denial, cancellation and unbounded-target rejection. |
| Power/idle behavior | `read_idle_preferences` and `idle_preferences_transaction` expose bounded dim and suspend intervals through the shared ShellSettings lock and revision CAS. The Linux session owner directly replaces the production `IdleController` policy, starts a fresh idle interval and undims if necessary. The Windows winit owner installs the policy in its production `LiveShell` state. Both report configured/applied generations and pending state. | **Idle lock remains excluded security policy** and is preserved byte-for-byte with unrelated shell settings. Inhibitor ownership, authentication, shutdown/restart, immediate suspend and listener controls are absent from and rejected by the schema. | Controlled-clock replacement/undim, schema exclusion, cooperative writer exclusion, file replacement, protected-field preservation and runtime acknowledgement have portable tests. Native Windows dimming and real suspend remain open; tests must never suspend the user's active session. |
| Launcher favorites/history | `read_launcher_favorites` and `launcher_favorites_transaction` expose bounded installed-app catalog IDs and add/remove/reorder through the production launcher preference owner. Reads retain the existing 64-entry bounds; arbitrary preference contents, paths, and typed/key history are excluded. | Recent history remains private diagnostic state rather than an editable remote input log. Transactions use generation/prior validation and preserve unrelated preference fields. | Native visible launcher/panel pin updates, removed catalog identity, and presented-pixel acceptance remain open; bounded strings, local write race, and cancellation have portable coverage. |
| Keyboard shortcuts | Settings keyboard-shortcuts page is a catalog/projection, not evidence of an editable production shortcut store. Spec expressly defers emergency-key customization. | Expose current bounded shortcut projection and provenance; do not invent a persisted customization API without production authority. Dual-Control remains immutable. | Real physical/semantic routing and protected emergency chord exclusion; no input-sequence logging. |
| Renderer/environment policy | Existing renderer diagnostics expose configured/effective mode/fallback; many knobs are environment/test-owned rather than Settings transactions. | Observe supported renderer policy/provenance; do not treat process env flags, test injection, backend override, arbitrary shader/source paths or source-tree builds as writable settings authority. A new mutation requires a real production owner, not a raw env setter. | Actual renderer transitions only where supported, stale/unsupported state, no fabricated live-mode change from updating a config string. |

## Explicit protected exclusions

`remote-ai-control.toml` and all listener/lease/pairing/audible-indication/Stop controls; update trust and arbitrary update installation/restart policy; `MaintenanceAction::SetPermission`, secure-storage recovery/secrets, credentials and identity; lock/security policy; arbitrary shell execution/source-tree/build/debug access; destructive storage policy; trusted indicator and emergency customization. These must be excluded field-by-field, not by making every page containing any protected field inaccessible to typed safe settings.

`MaintenanceService::inspect` mixes update/protection/permission/secure-storage status. A bounded diagnostic projection may include coarse capability/health where allowed, but must not forward its entire rich snapshot or all `MaintenanceAction` variants. `CheckForUpdates` is a possible benign owned re-query, not permission to install updates/change trust or invoke recovery.

## Safe production diagnostic actions checklist

| Action | Current authority/evidence | Remaining work |
|---|---|---|
| Repaint | `DiagnosticAction::Repaint` -> session damage/redraw scheduling; outcome explicitly does not confirm presentation. | Retain lease/epoch ownership through queued renderer dispatch; prove native frame-generation change and no protected capture. |
| Scene refresh | `RefreshScene` bounds windows/outputs and uses production scene/output housekeeping. | Verify exact focus/membership effects under live failure; do not label this a general platform/cache refresh. |
| Bounded frame trace start/stop | Existing per-lease authorized frame trace, category supplied by actual backend. | Other requested diagnostic categories remain separate gaps; no keystroke/text capture. |
| Diagnostic snapshot creation | `diagnostic_snapshot` already returns correlated bounded state. | An in-memory response satisfies “creation” without adding arbitrary filesystem export. Every asynchronous field still needs freshness/unavailable evidence. |
| Output identification | Local `IdentifyOutputs` and typed full-debug `diagnostic_action.identify_output` share `begin_output_identification`/the production badge renderer on DRM and winit. | Exact output incarnation; existing number only; three-second lifetime and one retained timer; lease/cancellation cleanup cannot clear newer local labels. Trusted controls remain above badges. Acknowledgement reports requested ownership, not presentation. Native acceptance must inspect displayed pixels; nested evidence does not prove DRM scanout. |
| Platform adapter re-query | `diagnostic_action.refresh_platform_status` accepts the exact `connectivity`, `audio`, `peripherals`, `maintenance`, and `default_associations` domains. Linux runs acknowledged immediate NetworkManager/BlueZ or PipeWire reads on their production workers without publishing prepared state; Windows performs bounded native status reads for all five domains. Peripheral, maintenance, and default-association refresh reuse their production services; Linux helper processes have owned process-group deadlines and bounded output, while Secret Service uses the bounded authenticated D-Bus transport. Windows maintenance starts PowerShell suspended and assigns it to a kill-on-close job before execution, with bounded output and live permit/deadline polling. Association refresh queries a fixed client-independent set rather than accepting targets. Private snapshots are reduced to coarse status before owner delivery. The owner rechecks the original lease, reserves shared input, rejects active physical/shell/remote interaction, and only then reconciles shell-backed snapshots or accepts a coarse observation. Collection and retention cap native objects, text, helper time, helper output, and D-Bus traffic. The payload cannot select services, paths, interfaces, association keys, permissions, credentials, pairing, storage, or mutation actions. | Outcome exposes only generation, preparation time, coarse availability/counts/health, partial status, and truthful owner reconciliation. It excludes SSIDs, device identities, jobs, mount and filesystem paths, provider and distribution names, association and handler identities, permission identities, errors, commands, and credential state. Feature probes remain a separate allowlisted domain. Complete live revocation/delay and presented Control Center acceptance, plus native Windows execution. |
| Application/icon inventory refresh | `diagnostic_action.refresh_application_inventory` prepares the production platform discovery on one bounded diagnostic worker, caps scanned entries, accepted applications, aggregate retained metadata, and recursive Windows depth, then rechecks the original full-debug lease before the compositor owner publishes Linux run signatures and reconciles launcher/panel state. The owner reserves shared input, rejects physical or shell interaction, advances an inventory generation, and invalidates launcher icon caches. The path-free request exposes no scan root, executable, icon-theme, or limit policy. | The outcome confirms owner reconciliation and reports count, partial discovery, generation, and preparation time; it does not claim renderer presentation. Complete live cancellation and presented launcher/icon acceptance on Linux, plus native Windows discovery/reconciliation acceptance. |
| Feature-health retry | The coherent diagnostic snapshot exposes the current compositor-owned Codex support, installation class, enabled state, health and configuration generation without source or account data. Settings Codex probe and optional-feature runtime acknowledgement paths remain their production owners. | Explicit retry remains excluded until each configured source kind has a bounded, non-mutating authority. Probe of an arbitrary custom executable or remote host is not a generic diagnostic action. Do not fold installation, credential recovery or enablement broadening into a diagnostic retry. |

## Recommended next self-contained domain

**Application scaling/toolkit compatibility** offers the highest direct debugging value after shell appearance: it addresses the project's live DPI/font-resizing work and already has typed policy, owned previous/applied values, bounded config loading, backend capability reporting, and verification-aware reset operations. Scope the implementation to the complete existing domain, including actual GTK/Qt writes/reset and truthful partial/restart outcomes—not just persisting a launch-environment flag. Start by extracting preparation/commit from `SettingsApp::apply_application_scale_policy` so both local Settings and MCP use one production transaction owner. The key missing contract is cancellation-safe external toolkit write ownership; audit `ToolkitScaleBackend::write` before exposing it.

A smaller follow-on with fewer external side effects is terminal **presentation** preferences (font/color/cursor/scrollback), but that is only part of the terminal domain; executable/directory settings remain explicitly outstanding. Display-layout transactions are high priority but less self-contained because safe rollback currently lives in the Settings UI, requiring movement into native owner policy first.

## Required evidence for each implemented domain

1. Typed schema rejects unknown/protected fields and arbitrary executable/path payloads.
2. Read reports bounded configured/effective values, source, generation, timestamp, and unavailable/pending/partial state.
3. Fresh prior/generation and underlying external revision checks prevent overwriting local changes; ABA and stale worker replies tested.
4. Preparation is bounded/off-owner; final mutation checks full-debug lease, protected state, shared input, request lifetime, emergency epoch and expiry immediately at commit.
5. Delay the actual worker/backend and revoke/timeout/disconnect; release delay and prove no late mutation, no stuck admission/input, and no silently extended lease.
   Separately lose or delay the completion receipt after native acceptance: the
   result must remain uncertain, retaining any ownership intent. Only a proven
   pre-submission rejection may clear that intent while still authorized. Generic
   string-error conversion is not evidence that a setter was never submitted.
6. Confirm the real production result natively (file/read-back/runtime/visible surface as appropriate), including restoration and failure outcome. Unit tests alone do not prove platform writes or rendering.
7. Exercise supported Linux and Windows owners separately; unsupported backend remains explicit, never inferred complete from Linux or cross-compilation.
8. Metrics use fixed domain/method/outcome labels; no values, paths, app names, network secrets or typed payloads retained.
